{{!-- Data: { HandlerName, RequestType, ResponseType } --}}
export class {{{HandlerName}}} {
  handle(request: {{{RequestType}}}): {{{ResponseType}}} {
    throw new Error('Not implemented');
  }

  // scaffold:inject
}
