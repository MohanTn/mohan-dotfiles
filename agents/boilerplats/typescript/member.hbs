{{!-- Data: { Signature, Body } — generic member for --inject into a scaffold:inject-marked block, e.g. Signature "async function listOrders(req: Request): Promise<void>". Body optional; default is a TODO throw. Indentation is relative to the member's own first line; the injector re-indents the block to the marker's depth. --}}
{{{Signature}}} {
  {{#if Body}}{{{Body}}}{{else}}throw new Error('TODO: implement');{{/if}}
}
