{{!-- Data: { ModuleUnderTest }. ModuleUnderTest is the import specifier, e.g. "./orders.js"
     (ESM specifiers need the .js extension even for a .ts source). Test files (*.test.ts) are
     exempt from the boilerplate guard, so new test() cases can be added with a plain Edit above
     the marker. Inject marker: // scaffold:inject --}}
import test from 'node:test';
import assert from 'node:assert/strict';
import * as subject from '{{{ModuleUnderTest}}}';

// scaffold:inject
