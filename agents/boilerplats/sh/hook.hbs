{{!-- Data: { HookEvent, Description, Matcher }. Matcher optional (e.g. "Edit|Write"; omit for
     events with no matcher, like UserPromptSubmit or SessionStart). Not currently gated by
     boilerplate-guard.sh (no sh content signature exists for a hook script), so this template
     exists to standardize the shape (stdin JSON, common.sh, exit code) rather than for
     enforcement. Inject marker: # scaffold:inject --}}
#!/usr/bin/env bash
# {{{HookEvent}}}{{#if Matcher}}: {{{Matcher}}}{{/if}} — {{{Description}}}
input=$(cat)
export HOOK_INPUT="$input"
source "$HOME/.claude/hooks/lib/common.sh"

# scaffold:inject

exit 0
