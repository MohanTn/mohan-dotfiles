{{!-- Data: { TestSubject, Description }. TestSubject is the hook script under test, e.g.
     "pre-tool-use.sh". Matches the copilot/tests/*.test.sh shape: sources the shared lib.sh
     harness (run_hook/expect_out/expect_silent/payload_tool), ends with summary(). This file
     is itself exempt from boilerplate-guard.sh (test files are), so new expect_* cases can be
     added with a plain Edit above the marker. Inject marker: # scaffold:inject --}}
#!/usr/bin/env bash
# {{{TestSubject}}} — {{{Description}}}
source "$(dirname "${BASH_SOURCE[0]}")/lib.sh"

# scaffold:inject

summary
