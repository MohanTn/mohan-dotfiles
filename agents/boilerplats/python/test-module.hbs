{{!-- Data: { ModuleUnderTest, ClassName }. ModuleUnderTest is a description for the docstring,
     e.g. "context-augment.py". Test files (test_*.py) are exempt from the boilerplate guard, so
     new test methods can be added with a plain Edit above the marker — scaffold_inject with
     template "member" also works if you want a generated stub. Inject marker: # scaffold:inject --}}
#!/usr/bin/env python3
"""Unit tests for {{{ModuleUnderTest}}}."""
import unittest


class {{{ClassName}}}(unittest.TestCase):
    # scaffold:inject
    pass


if __name__ == "__main__":
    unittest.main()
