{{!-- Data: { Package, EntityName, ServiceType, ServiceField }. The marker sits at FILE scope, not inside RegisterRoutes: Go has no nested method declarations, so an injected handler method must land outside it. Route registrations go inside RegisterRoutes as ordinary edits. --}}
package {{{Package}}}

import "net/http"

type {{{EntityName}}}Controller struct {
	{{{ServiceField}}} {{{ServiceType}}}
}

func New{{{EntityName}}}Controller({{{ServiceField}}} {{{ServiceType}}}) *{{{EntityName}}}Controller {
	return &{{{EntityName}}}Controller{
		{{{ServiceField}}}: {{{ServiceField}}},
	}
}

func (c *{{{EntityName}}}Controller) RegisterRoutes(mux *http.ServeMux) {
	// TODO: register handlers, e.g. mux.HandleFunc("/", c.list)
}

// scaffold:inject
