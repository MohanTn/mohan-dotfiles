{{!-- Data: { ModuleUnderTest }. ModuleUnderTest is the require path, e.g. "./orders". Test files
     (*.test.js) are exempt from the boilerplate guard, so new test() cases can be added with a
     plain Edit above the marker. Inject marker: // scaffold:inject --}}
'use strict';

const test = require('node:test');
const assert = require('node:assert/strict');
const subject = require('{{{ModuleUnderTest}}}');

// scaffold:inject
