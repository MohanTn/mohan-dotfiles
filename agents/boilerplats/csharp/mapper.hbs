{{!-- Data: { Namespace, SourceType, TargetType } --}}
namespace {{{Namespace}}}.Mappers;

public static class {{{SourceType}}}To{{{TargetType}}}Mapper
{
    public static {{{TargetType}}} Map({{{SourceType}}} source)
    {
        throw new NotImplementedException();
    }

    // scaffold:inject
}
