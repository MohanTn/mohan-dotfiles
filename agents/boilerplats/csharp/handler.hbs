{{!-- Data: { Namespace, HandlerName, RequestType, ResponseType } --}}
namespace {{{Namespace}}}.Handlers;

public class {{{HandlerName}}}
{
    public {{{ResponseType}}} Handle({{{RequestType}}} request)
    {
        throw new NotImplementedException();
    }

    // scaffold:inject
}
